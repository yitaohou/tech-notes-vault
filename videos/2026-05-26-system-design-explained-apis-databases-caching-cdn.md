---
video_id: oYxTTirKY8M
url: https://www.youtube.com/watch?v=oYxTTirKY8M
title: 'System Design Explained: APIs, Databases, Caching, CDNs, Load Balancing &
  Production Infra'
channel: Hayk Simonyan
published: '2026-05-26'
duration: '2:04:06'
transcript_origin: subs
tags:
- video
---

# System Design Explained: APIs, Databases, Caching, CDNs, Load Balancing & Production Infra

摘要: [[2026-05-26-system-design-explained-apis-databases-caching-cdn-summary|完整摘要]]

## 知识点

- [[access-control-list]] — ACL 为特定资源（如一份文档）单独维护权限列表，明确记录哪些用户拥有访问权、以及各自具体拥有读或写等哪些权限。（[1:50:20](https://youtu.be/oYxTTirKY8M?t=6620)）
- [[access-control-list-acl]] — ACL（access control list）允许为具体资源（如某一份文档）单独设置每个用户的访问权限，而不是像 RBAC 那样按角色统一授权。（[1:51:32](https://youtu.be/oYxTTirKY8M?t=6692)）
- [[access-refresh-token-pattern]] — 用户登录成功后会同时拿到 access token 和 refresh token，access token 过期时客户端返回 unauthorized 响应，此时用之前存下的 refresh token 向 auth server 请求新 access token，从而免于让用户重新输入凭证。（[96:26](https://youtu.be/oYxTTirKY8M?t=5786)）
- [[acid-transaction]] — SQL transaction 是作为单一原子单元（atomic unit）执行的一个或多个 SQL 操作组成的序列。（[06:00](https://youtu.be/oYxTTirKY8M?t=360)）
- [[acid-transactions]] — ACID 事务的 atomicity 指整个事务被当作单一单元处理，要么完全成功，要么完全失败。（[09:00](https://youtu.be/oYxTTirKY8M?t=540)）
- [[amqp]] — AMQP 是常用于异步通信场景的一种应用层协议。（[45:08](https://youtu.be/oYxTTirKY8M?t=2708)）
- [[amqp-producer-consumer-model]] — 在 AMQP 中，producer（例如 web service 或支付系统）把消息发布到 message broker，broker 内部维护 queue，consumer（例如支付处理器或通知系统）再从队列中取出消息进行处理。（[51:09](https://youtu.be/oYxTTirKY8M?t=3069)）
- [[amqp-protocol]] — AMQP 允许在 consumer 和 producer 之间加入 message queue，从而实现异步通信（asynchronous communication）。（[57:11](https://youtu.be/oYxTTirKY8M?t=3431)）
- [[api-abstraction-implementation-hiding]] — 调用方通过 API 发起请求（例如保存用户数据）时完全不需要了解服务端内部的实现逻辑，只需使用提供好的 endpoint 即可完成操作。（[30:03](https://youtu.be/oYxTTirKY8M?t=1803)）
- [[api-definition]] — API 本质上是客户端（如浏览器、手机 App）与服务端之间的一份契约，定义了可用的 endpoint、可使用的方法，以及针对每个 endpoint 能期望得到的响应。（[30:03](https://youtu.be/oYxTTirKY8M?t=1803)）
- [[api-design-consistency-principle]] — 好的 API 设计的首要原则是一致性（consistency），即在整个 API 中统一使用相同的命名规范、大小写风格（如统一使用 camelCase）和设计模式。（[38:00](https://youtu.be/oYxTTirKY8M?t=2280)）
- [[api-design-process-requirements-gathering]] — API 设计流程首先要理解需求，具体做法是识别核心用例（use case）和用户故事（user story）。（[42:08](https://youtu.be/oYxTTirKY8M?t=2528)）
- [[api-development-lifecycle]] — API 开发不只是写代码，还包括设计阶段、后续维护，以及生命周期结束时的下线处理。（[45:08](https://youtu.be/oYxTTirKY8M?t=2708)）
- [[api-endpoint-filtering]] — 真实场景中很少一次性返回全部结果，因此常通过查询参数（如 category、in_stock=true）对集合类接口的返回结果进行过滤。（[63:13](https://youtu.be/oYxTTirKY8M?t=3793)）
- [[api-key-authentication]] — API key authentication 中，服务端为每个 client 生成一个唯一的 key，client 之后每次请求都携带该 key（通过 Authorization header 或专门的 X-API-Key header）来访问资源。（[87:22](https://youtu.be/oYxTTirKY8M?t=5242)）
- [[api-key-security-limitations]] — API key 一旦泄露，任何人都可以冒充持有者访问资源，且 API key 默认没有内置过期机制，除非开发者自行实现。（[90:23](https://youtu.be/oYxTTirKY8M?t=5423)）
- [[api-key-vs-jwt-distinction]] — API key 只是随机字符串、不含任何嵌入信息，而 JWT 可以直接携带身份和权限数据，因此使用 API key 时服务器必须额外查库才能得知调用者是谁、拥有什么权限。（[90:23](https://youtu.be/oYxTTirKY8M?t=5423)）
- [[api-lifecycle-management]] — API 生命周期依次经历设计（design）、开发（development）、部署监控（deploy and monitor）、维护（maintenance）、废弃退役（deprecation and retirement）五个阶段。（[42:08](https://youtu.be/oYxTTirKY8M?t=2528)）
- [[api-naming-convention-consistency]] — 同一套 API 中所有端点应统一使用同一种命名规范，例如不能一个端点用 camelCase（如 userDetails），另一个端点又用 snake_case（如 user/details），否则会破坏一致性。（[39:07](https://youtu.be/oYxTTirKY8M?t=2347)）
- [[api-payload-minimization]] — 出于性能考虑，API 返回的数据体量（payload）应尽量精简，避免返回超出客户端实际需要的数据。（[40:35](https://youtu.be/oYxTTirKY8M?t=2435)）
- [[api-performance-requirements-identification]] — API 设计流程中需要确定性能要求，具体分析该 API 可能存在哪些瓶颈，以及哪些部分必须保证高性能。（[42:08](https://youtu.be/oYxTTirKY8M?t=2528)）
- [[api-predictable-behavior-principle]] — 好的 API 端点不应产生超出预期范围的副作用，例如一个用于获取用户详情的 GET 请求不应该顺带更新关注者数据。（[38:10](https://youtu.be/oYxTTirKY8M?t=2290)）
- [[api-protection-techniques-overview]] — 保护 API 免受攻击有七种常见的经过验证的技术，rate limiting 是其中第一种。（[1:54:33](https://youtu.be/oYxTTirKY8M?t=6873)）
- [[api-protocol-design-influence]] — API 底层选用的协议（HTTP、WebSocket、gRPC 等）会从根本上决定其设计方式、性能与能力范围，因此协议选型应基于各协议自身的优劣势来确定。（[41:10](https://youtu.be/oYxTTirKY8M?t=2470)）
- [[api-protocol-selection-criteria]] — 选择 API 协议时首先要考虑交互模式：默认请求-响应场景用 HTTP，而实时通信（如实时聊天）场景则需要用 WebSocket。（[54:11](https://youtu.be/oYxTTirKY8M?t=3251)）
- [[api-round-trip-reduction]] — 如果明确知道客户端后续会用到某些数据，可以在当前请求的响应中顺带一起返回，以减少额外的请求往返（round trip），而不必为此单独新建一个接口。（[40:50](https://youtu.be/oYxTTirKY8M?t=2450)）
- [[api-scope-boundary-definition]] — 对于体量较大的 API，通常无法一次性开发全部功能，因此需要明确 scope（本次要做的功能）与哪些功能属于 out of scope（暂不做）。（[42:08](https://youtu.be/oYxTTirKY8M?t=2528)）
- [[api-security-constraints-baseline]] — API 设计需要考虑安全约束，至少要实现 authentication、authorization、rate limiting 这类基础功能，具体还需根据 API 用途补充更多安全措施。（[42:08](https://youtu.be/oYxTTirKY8M?t=2528)）
- [[api-security-pillars]] — API 安全性至少需要具备用户间的 authentication 与 authorization、对输入数据的校验（validation），以及 rate limiting 这三项基本措施。（[39:55](https://youtu.be/oYxTTirKY8M?t=2395)）
- [[api-service-boundary]] — API 为系统划定了清晰的服务边界，因此可以拆分出多个各自独立负责的服务（如专门管理用户的服务、专门管理帖子的服务），不同服务或客户端与服务端之间可以在互不了解底层实现的前提下正常通信。（[30:03](https://youtu.be/oYxTTirKY8M?t=1803)）
- [[api-simplicity-principle]] — 好的 API 设计标准是：开发者甚至不需要阅读文档，就能快速理解并直接使用它，这要求设计上尽量降低复杂度、聚焦核心用例。（[39:30](https://youtu.be/oYxTTirKY8M?t=2370)）
- [[api-sorting-parameter]] — 好的 REST API 应支持排序（sorting）参数，让客户端可以指定按价格或评分等字段对返回的资源列表排序。（[76:15](https://youtu.be/oYxTTirKY8M?t=4575)）
- [[api-sorting-query-parameter]] — 服务端排序功能通过 query parameter（如 sort 属性，取值可为按价格升序、按评论数升序或降序等）实现，客户端传入该参数即可拿到已排好序的结果。（[1:06:15](https://youtu.be/oYxTTirKY8M?t=3975)）
- [[api-style-taxonomy]] — 目前最主流的三种 API 风格是 RESTful、GraphQL 和 gRPC。（[45:08](https://youtu.be/oYxTTirKY8M?t=2708)）
- [[api-usable-without-documentation-principle]] — 衡量 API 设计好坏的一个标准是：最好的 API 应该是无需阅读文档即可正确使用的 API。（[37:55](https://youtu.be/oYxTTirKY8M?t=2275)）
- [[api-versioning]] — REST API 请求通常带有 /api/v1、/api/v2 这样的版本前缀；当未来迁移 API 并在新版本中引入可能破坏旧功能的改动时，仍在使用旧版本号的前端不会受到影响，可以继续使用旧版本的功能。（[76:25](https://youtu.be/oYxTTirKY8M?t=4585)）
- [[application-layer-protocol]] — 应用层协议构建在 TCP、UDP 等传输层协议之上，负责定义消息格式与结构、请求响应模式，以及连接管理和错误处理。（[45:08](https://youtu.be/oYxTTirKY8M?t=2708)）
- [[attribute-based-access-control]] — ABAC 通过检查用户模型或资源模型中的具体属性（而非固定角色）来决定允许或拒绝访问，例如仅当用户部门为 HR 时才授予某项访问权限。（[1:49:15](https://youtu.be/oYxTTirKY8M?t=6555)）
- [[auth-gate-before-processing]] — 在用户或服务能够访问 API gateway、服务层和数据存储之前，必须先通过 authentication（如发送登录请求），身份确认无效则以 401 unauthorized 拒绝。（[84:20](https://youtu.be/oYxTTirKY8M?t=5060)）
- [[authentication-authorization-conflation]] — 许多工程师会把 JWT 这类身份认证方法和 authorization 框架混为一谈，错误地把 JWT 当成 authorization 方案本身。（[81:20](https://youtu.be/oYxTTirKY8M?t=4880)）
- [[authentication-definition]] — authentication 要解决的核心问题只有一个：确认发起请求的用户或服务的身份是谁。（[84:20](https://youtu.be/oYxTTirKY8M?t=5060)）
- [[authentication-methods-taxonomy]] — authentication 方法可从基础到复杂依次划分为：basic 与 digest authentication、API keys、session 与 cookie、bearer authentication 与 JWT token，以及更高层的 OAuth 2、OpenID Connect 和 single sign-on。（[84:20](https://youtu.be/oYxTTirKY8M?t=5060)）
- [[authentication-service-login-flow]] — 客户端首次提交凭据后，认证服务校验合法性并生成 JWT 返回给客户端；此后客户端的后续请求都在 authorization header 中携带该 bearer token 完成认证。（[93:24](https://youtu.be/oYxTTirKY8M?t=5604)）
- [[authentication-vs-authorization]] — Authentication 的作用是在系统能够做出任何授权或限制决策之前，先验证发起请求的用户（通过浏览器或移动端）或第三方服务的身份是否与其所声称的一致。（[81:20](https://youtu.be/oYxTTirKY8M?t=4880)）
- [[authorization-header-token-transport]] — 客户端请求携带的 authorization header 中包含认证类型（如 bearer）以及具体的 token 值，服务端据此校验该 token。（[93:24](https://youtu.be/oYxTTirKY8M?t=5604)）
- [[autoscaling]] — 云托管负载均衡器（如 AWS Elastic Load Balancing）自动内置 autoscaling 能力，能在应用需求增加时自动向服务器池中添加新实例。（[24:03](https://youtu.be/oYxTTirKY8M?t=1443)）
- [[basic-authentication]] — Basic authentication 的流程是服务端在凭证校验通过后返回 200 OK 及用户数据，校验失败则返回 401 unauthorized。（[87:22](https://youtu.be/oYxTTirKY8M?t=5242)）
- [[basic-authentication-flow]] — basic authentication 是最简单的认证形式：首次请求资源会收到 401 unauthorized，随后在后续请求中通过 authorization header 携带 base64 编码后的凭据完成认证。（[84:20](https://youtu.be/oYxTTirKY8M?t=5060)）
- [[bearer-jwt-confusion]] — bearer authentication 常与 JWT 混淆，但 JWT 本质上只是一种 token 格式，二者概念并不等同。（[84:20](https://youtu.be/oYxTTirKY8M?t=5060)）
- [[bearer-token-pattern]] — 开发者常混淆 bearer token 与 JWT：bearer token 只是一种模式（谁拥有该 token 谁就能访问），而 JWT 是 bearer token 最常见的具体实现类型。（[93:24](https://youtu.be/oYxTTirKY8M?t=5604)）
- [[bottom-up-api-design-approach]] — bottom-up 设计方式指若已存在数据模型和系统能力，就应基于这些已有基础来设计 API，常见于已有实际产品架构的公司环境，而非面试场景。（[42:08](https://youtu.be/oYxTTirKY8M?t=2528)）
- [[citrix-load-balancer]] — Citrix 也提供负载均衡功能，属于硬件类型的负载均衡器。（[24:03](https://youtu.be/oYxTTirKY8M?t=1443)）
- [[consistent-hashing-failover]] — consistent hashing 使用哈希函数把各台服务器以及基于用户 IP 生成的请求映射到一个环形哈希空间（hash ring）上，请求会被路由到哈希环上离其映射点最近的服务器。（[21:03](https://youtu.be/oYxTTirKY8M?t=1263)）
- [[contract-first-api-design-approach]] — contract-first 设计方式先定义好请求和响应应该长什么样的 API 契约，再着手实现，思路上与 top-down 方式类似，也常用于面试场景。（[42:08](https://youtu.be/oYxTTirKY8M?t=2528)）
- [[cors-cross-origin-resource-sharing]] — 若 API 仅供自己的前端域名（如 app.yourdomain.com）调用，则应配置 CORS 只允许该来源的请求，来自其他域名（如 app.anotherdomain.com）的请求应被拒绝。（[1:57:34](https://youtu.be/oYxTTirKY8M?t=7054)）
- [[crud-http-method-mapping]] — REST API 依托 HTTP 协议，用 GET/POST/PUT/PATCH/DELETE 这几种 HTTP 方法分别实现读取、创建、更新（整体/部分）、删除这些最常见的 CRUD 操作。（[69:17](https://youtu.be/oYxTTirKY8M?t=4157)）
- [[csrf-attack]] — 若银行系统仅依赖 session cookie 做身份验证而缺乏额外校验，恶意网站可以利用用户浏览器中已有的 cookie 冒充其身份，偷偷提交转账等请求。（[2:00:34](https://youtu.be/oYxTTirKY8M?t=7234)）
- [[csrf-token-defense]] — 服务端同时校验 session cookie 是否存在、以及请求携带的 CSRF token 是否与服务端记录匹配，只有两者都通过才放行请求，从而拦截来自未知第三方的伪造请求，同时放行用户本人的合法请求。（[2:00:34](https://youtu.be/oYxTTirKY8M?t=7234)）
- [[cursor-based-pagination]] — cursor-based pagination 用一个 cursor（代表要获取页面位置的哈希值）取代 page 和 limit 参数，是分页实现的第三种常见方案。（[1:06:15](https://youtu.be/oYxTTirKY8M?t=3975)）
- [[ddos-botnet-rate-limit-bypass]] — 即使对每个用户或 IP 设置了 rate limit（例如每 IP 100 次请求），攻击者仍可通过启动大量 bot、让每个 bot 各自把请求量控制在限额之内，从而使总请求量远超系统承载能力。（[1:57:34](https://youtu.be/oYxTTirKY8M?t=7054)）
- [[digest-authentication]] — Digest authentication 的流程与 Basic authentication 类似：先返回 401 unauthorized 提示客户端携带凭证，客户端随后发送包含用户名密码 MD5 哈希值的请求，校验通过则返回用户数据，否则仍返回 401。（[87:22](https://youtu.be/oYxTTirKY8M?t=5242)）
- [[dns-domain-to-ip-resolution]] — 当用户在浏览器或 App 中访问类似 app.demo.com 的域名时，设备会先向 DNS 查询，DNS 返回该域名对应的服务器 IP 地址后，客户端才能据此向服务器发送 HTTP 请求。（[03:00](https://youtu.be/oYxTTirKY8M?t=180)）
- [[document-store-database]] — 文档存储数据库（如 MongoDB）以类似 JSON 的文档存储数据，允许在单条记录内容纳复杂的数据结构。（[09:32](https://youtu.be/oYxTTirKY8M?t=572)）
- [[f5-load-balancer]] — F5 是知名的硬件负载均衡器，以高性能和丰富的功能集著称，区别于 Nginx、HAProxy 等软件负载均衡器。（[24:03](https://youtu.be/oYxTTirKY8M?t=1443)）
- [[filtering-sorting-pagination-benefits]] — 同时支持 filtering、sorting、pagination 的 API 设计能节省服务器带宽、同时提升服务端与前端两侧的性能，并让前端在拉取数据时拥有更大的灵活性。（[1:06:15](https://youtu.be/oYxTTirKY8M?t=3975)）
- [[geo-based-load-balancing]] — 地理位置负载均衡会为不同地区（如 US East、US West、Europe）分别部署服务器，并根据请求方 IP 所在地域将其导向地理上最近的服务器节点。（[21:03](https://youtu.be/oYxTTirKY8M?t=1263)）
- [[github-repo-access-permission-tiers]] — 在 GitHub 仓库权限设计中，write access 用户只能 push 代码，read access 用户只能查看仓库但不能 push 或创建 pull request，admin 用户则拥有包括删除仓库在内的完全控制权。（[1:45:30](https://youtu.be/oYxTTirKY8M?t=6330)）
- [[graph-database]] — 图数据库专注于以图（graph）的形式存储实体及实体间的关系，Neo4j 是典型代表。（[10:03](https://youtu.be/oYxTTirKY8M?t=603)）
- [[graphql]] — GraphQL 只提供一个单一端点（single endpoint）处理所有操作，客户端通过请求中的 payload 精确指定自己想要获取的数据字段。（[33:05](https://youtu.be/oYxTTirKY8M?t=1985)）
- [[graphql-application-level-caching]] — 由于 GraphQL 使用单一端点，无法像 REST 那样直接利用 HTTP caching，因此需要采用 application-level caching。（[37:40](https://youtu.be/oYxTTirKY8M?t=2260)）
- [[graphql-client-specifies-response-shape]] — 使用 GraphQL 时，响应的具体结构由客户端在请求中指定，不像 REST 那样服务端预先固定好统一的响应结构。（[37:24](https://youtu.be/oYxTTirKY8M?t=2244)）
- [[graphql-error-handling]] — GraphQL API 无论是否出错都始终返回 200 OK 状态码，因此错误必须通过响应体中的 errors 字段表示，并可在 errors 中携带具体的状态码（如 404）和错误信息。（[81:20](https://youtu.be/oYxTTirKY8M?t=4880)）
- [[graphql-mutation]] — 任何对数据库进行写操作（如 createUser）的 GraphQL 请求都通过 mutation 完成，mutation 同样需要定义输入参数和返回类型。（[78:18](https://youtu.be/oYxTTirKY8M?t=4698)）
- [[graphql-mutation-input-types]] — GraphQL 最佳实践建议在 mutation 中使用专门的 input type 来传递写入参数，而不是直接罗列离散的参数字段。（[81:20](https://youtu.be/oYxTTirKY8M?t=4880)）
- [[graphql-naming-convention]] — GraphQL 最佳实践强调为 type 和 field 使用有意义的命名，因为客户端和服务端共享同一份 schema，清晰命名有助于双方理解数据结构。（[81:20](https://youtu.be/oYxTTirKY8M?t=4880)）
- [[graphql-operation-types]] — GraphQL 用 query 表示数据读取操作，用 mutation 表示数据更新操作（相当于 RESTful 中的 put、patch 或 post），并额外提供 subscription 操作用于实时通信。（[33:05](https://youtu.be/oYxTTirKY8M?t=1985)）
- [[graphql-origin-motivation]] — GraphQL 由 Facebook 发明，用于解决客户端对多个 REST API（如 user、post、comments、likes）分别请求却仍需多次调用才能拼齐所需数据的问题，这会累积增加页面整体延迟。（[78:18](https://youtu.be/oYxTTirKY8M?t=4698)）
- [[graphql-query]] — GraphQL query 需要声明函数名、输入参数（如 user ID）和返回类型（如返回 schema 中定义的 User 类型），是 GraphQL 读取数据的标准方式。（[78:18](https://youtu.be/oYxTTirKY8M?t=4698)）
- [[graphql-query-depth-limiting]] — GraphQL 查询可能出现无限嵌套（如 user 嵌套 post 再嵌套 comment），最佳实践是设置查询深度限制（如最多6到7层）来避免过深的嵌套查询。（[81:20](https://youtu.be/oYxTTirKY8M?t=4880)）
- [[graphql-query-language]] — GraphQL 的查询语言允许客户端在一次请求中精确声明需要哪些字段（例如只取 posts 的 title 和 content），从而避免获取多余数据。（[36:40](https://youtu.be/oYxTTirKY8M?t=2200)）
- [[graphql-round-trip-reduction]] — 在 RESTful API 中可能需要三次独立请求才能获取的关联数据，用 GraphQL 只需一次请求即可全部拿到，从而避免了不必要的多次往返。（[33:05](https://youtu.be/oYxTTirKY8M?t=1985)）
- [[graphql-schema-contract]] — GraphQL schema 中定义类型（type）及其全部字段，若字段本身不是原始类型（如 posts 是一个 Post 数组），则该类型可以单独在 schema 中再定义。（[78:18](https://youtu.be/oYxTTirKY8M?t=4698)）
- [[graphql-schema-domain-mirroring]] — 好的 GraphQL schema 设计应当镜像实际的业务领域模型（domain model），并做到直观（intuitive）且灵活（flexible）。（[78:18](https://youtu.be/oYxTTirKY8M?t=4698)）
- [[graphql-schema-evolution-without-versioning]] — GraphQL 的 schema 通常不采用像 REST 那样的整体版本号（V1、V2）迭代方式，而是可以直接修改字段，或对个别字段做增量版本化（如新增 followersV2 字段）。（[37:24](https://youtu.be/oYxTTirKY8M?t=2244)）
- [[graphql-schema-modularity]] — GraphQL 最佳实践建议将 schema 设计得小而模块化，便于维护和扩展。（[81:20](https://youtu.be/oYxTTirKY8M?t=4880)）
- [[graphql-single-endpoint]] — GraphQL 与 REST 不同，它通常只用一个单一端点（如 /graphql）来处理所有操作，而不是像 REST 那样为不同资源分别设置端点。（[36:24](https://youtu.be/oYxTTirKY8M?t=2184)）
- [[graphql-vs-rest-request-efficiency]] — 对于同一份跨用户、帖子、关注者等多个资源的数据需求，GraphQL 可以用一次请求完成，而 RESTful API 通常需要发起三次独立请求。（[37:10](https://youtu.be/oYxTTirKY8M?t=2230)）
- [[grpc]] — gRPC 是 Google 的远程过程调用协议，是服务器间通信中常用的一种协议类型。（[45:08](https://youtu.be/oYxTTirKY8M?t=2708)）
- [[grpc-http2-transport]] — gRPC 使用 HTTP/2 作为传输层，这意味着客户端必须支持 HTTP/2 才能使用 gRPC 进行通信。（[54:11](https://youtu.be/oYxTTirKY8M?t=3251)）
- [[grpc-microservices-efficiency]] — gRPC 特别适合用于微服务之间以及内部系统的通信，因为在服务器与服务器之间的通信效率上，它比 GraphQL 或 RESTful API 更高效。（[33:05](https://youtu.be/oYxTTirKY8M?t=1985)）
- [[grpc-microservices-performance]] — gRPC 常用于架构内部微服务之间的通信，相比 HTTP 能带来更快的性能。（[42:00](https://youtu.be/oYxTTirKY8M?t=2520)）
- [[grpc-proto-files]] — gRPC 的方法（method）是在 proto 文件中以 RPC 的形式定义的。（[33:05](https://youtu.be/oYxTTirKY8M?t=1985)）
- [[grpc-protocol]] — gRPC 是由 Google 发明的高性能 RPC 框架，主要使用 protocol buffers 进行数据编码。（[54:11](https://youtu.be/oYxTTirKY8M?t=3251)）
- [[grpc-rpc-framework]] — gRPC 是三种主流 API 风格（REST、GraphQL、gRPC）中使用最少的一种，本质是使用 protocol buffers 通信的高性能 RPC 框架。（[33:05](https://youtu.be/oYxTTirKY8M?t=1985)）
- [[grpc-server-to-server-usage]] — 因为多数浏览器不支持最新版本的 HTTP，gRPC 在浏览器-服务器通信中并不常见，而更常用于多个微服务之间彼此通信的场景。（[54:11](https://youtu.be/oYxTTirKY8M?t=3251)）
- [[grpc-streaming-bidirectional]] — gRPC 原生支持流式（streaming）通信以及客户端与服务端之间的双向（bidirectional）通信。（[33:05](https://youtu.be/oYxTTirKY8M?t=1985)）
- [[haproxy]] — HAProxy 是与 Nginx 类似的开源软件负载均衡器，需要用户自行安装并配置。（[24:03](https://youtu.be/oYxTTirKY8M?t=1443)）
- [[health-check-load-balancing]] — 负载均衡器会实时判断各服务器当前的负载状况，将新到达的请求转发给负载最小的服务器。（[15:01](https://youtu.be/oYxTTirKY8M?t=901)）
- [[horizontal-scaling]] — 在负载均衡器的支持下，向系统新增第四台或更多服务器后，负载均衡器会自动把新服务器纳入流量分配范围，确保各实例负载均匀，从而提升系统的可扩展性。（[15:01](https://youtu.be/oYxTTirKY8M?t=901)）
- [[http-200-ok]] — 客户端成功获取到某个资源（如某个 product）时，应返回 HTTP 200 状态码表示请求成功。（[72:17](https://youtu.be/oYxTTirKY8M?t=4337)）
- [[http-201-created]] — 创建资源的 POST 请求成功后应返回 201（Created）而非 200，因为 200 只表示笼统成功，201 专门表示新资源已被创建。（[72:17](https://youtu.be/oYxTTirKY8M?t=4337)）
- [[http-204-no-content]] — HTTP 204 用于表示请求成功但没有内容返回，例如 DELETE 请求成功删除资源后可以返回 204。（[72:17](https://youtu.be/oYxTTirKY8M?t=4337)）
- [[http-300-redirect]] — 当请求的 URL 对应资源已经迁移到别处时，服务器返回 300 系列状态码并把客户端重定向到新 URL。（[72:17](https://youtu.be/oYxTTirKY8M?t=4337)）
- [[http-400-bad-request]] — HTTP 400 是通用的客户端错误状态码，例如请求参数无效或 JSON 格式错误时应返回 400。（[72:17](https://youtu.be/oYxTTirKY8M?t=4337)）
- [[http-404-not-found]] — 当客户端按 ID 查询某个资源（如某个 product）但数据库中不存在该记录时，应返回 404 状态码表示未找到该资源。（[72:17](https://youtu.be/oYxTTirKY8M?t=4337)）
- [[http-500-internal-server-error]] — HTTP 500 属于 5xx 服务端错误系列，用于客户端请求本身没有问题、但服务器内部出现未知异常的情况，通常会连带返回一条 server error 消息。（[72:17](https://youtu.be/oYxTTirKY8M?t=4337)）
- [[http-authentication-methods]] — HTTP 请求常用的身份验证方式包括 bearer token、basic authentication 和 OAuth 等。（[48:20](https://youtu.be/oYxTTirKY8M?t=2900)）
- [[http-common-headers]] — 常见的 HTTP header 包括 content type（通常由服务端定义）、authorization（客户端用于向服务端发起身份验证）、accept、cache control 和 user agent 等。（[50:00](https://youtu.be/oYxTTirKY8M?t=3000)）
- [[http-delete-method]] — DELETE 方法使用与更新操作相同的路径格式（/products/{id}），用于从数据库中删除对应资源，对应 CRUD 中的 Delete 操作。（[71:35](https://youtu.be/oYxTTirKY8M?t=4295)）
- [[http-get-method]] — GET 方法用于从 API 读取数据，例如检索 products 或 reviews 等资源，对应 CRUD 中的 Read 操作。（[69:17](https://youtu.be/oYxTTirKY8M?t=4157)）
- [[http-idempotency]] — GET 请求既是安全的又是幂等的（idempotent），意味着多次请求 /products 在数据库无变化时会返回完全相同的结果。（[69:40](https://youtu.be/oYxTTirKY8M?t=4180)）
- [[http-method-determines-resource-action]] — 同样是 /orders 这个 URL，用 GET 方法请求会返回订单列表，用 POST 方法请求则会创建一个新订单，动作语义完全由 HTTP 方法承担。（[63:13](https://youtu.be/oYxTTirKY8M?t=3793)）
- [[http-methods]] — HTTP 方法中，GET 用于获取数据，POST 用于创建数据，PUT 或 PATCH 用于更新数据（分别对应全量更新与部分更新），DELETE 用于删除数据。（[49:15](https://youtu.be/oYxTTirKY8M?t=2955)）
- [[http-methods-crud-mapping]] — REST 使用标准的 HTTP 协议方法作为操作语义：GET 用于获取数据，POST 用于存储数据，PUT 或 PATCH 用于更新数据，DELETE 用于删除数据。（[30:03](https://youtu.be/oYxTTirKY8M?t=1803)）
- [[http-patch-method]] — PATCH 方法与 PUT 使用相同的请求路径（/products/{id}），但只更新请求中提供的部分字段（如仅更新 title），其余属性保持不变。（[71:05](https://youtu.be/oYxTTirKY8M?t=4265)）
- [[http-post-method]] — POST 方法请求路径与 GET 相同（如 /products），但语义是创建资源而非检索，对应 CRUD 中的 Create 操作。（[70:10](https://youtu.be/oYxTTirKY8M?t=4210)）
- [[http-protocol]] — HTTP 是最常见的应用层协议，也是构建 Web API 的基础协议。（[45:08](https://youtu.be/oYxTTirKY8M?t=2708)）
- [[http-put-method]] — PUT 方法请求路径形如 /products/{id}，会用前端传来的新数据完整替换掉数据库中该 ID 对应的整条资源记录。（[70:45](https://youtu.be/oYxTTirKY8M?t=4245)）
- [[http-request-structure]] — 客户端发起的 HTTP 请求需要指定方法（如 GET、POST）以及资源 URL（例如 /api/products）。（[45:08](https://youtu.be/oYxTTirKY8M?t=2708)）
- [[http-response-structure]] — HTTP 响应包含与请求一致的 HTTP 版本、表示请求处理结果的状态码，以及 content type 等 header 信息（content type 常见值为 application/JSON，也可以是静态网页等）。（[48:40](https://youtu.be/oYxTTirKY8M?t=2920)）
- [[http-status-code-categories]] — HTTP 状态码按区间分类：2xx 表示请求成功，3xx 表示重定向，4xx 表示客户端错误，5xx 表示服务器端错误。（[49:40](https://youtu.be/oYxTTirKY8M?t=2980)）
- [[https-benefits]] — HTTPS 的主要好处包括传输过程中数据加密、保证数据完整性（data integrity）、在提供数据前对用户进行身份验证，以及带来 SEO 收益；只使用未加密的 HTTP 则存在较多安全风险。（[50:35](https://youtu.be/oYxTTirKY8M?t=3035)）
- [[https-golden-standard]] — 由于使用纯 HTTP 存在较多安全风险，服务器设计中的黄金标准是始终使用 HTTPS。（[50:45](https://youtu.be/oYxTTirKY8M?t=3045)）
- [[https-tls-ssl-definition]] — HTTPS 本质上是 HTTP 协议加上 TLS 或 SSL 加密与证书机制，用于在传输过程中保护数据安全。（[50:20](https://youtu.be/oYxTTirKY8M?t=3020)）
- [[incremental-system-design-approach]] — 学习系统设计时应从最简单的单用户场景开始搭建基础架构，再逐步扩展以应对更大规模，这样能在引入更多复杂性之前先理解每个核心组件的作用。（[00:00](https://youtu.be/oYxTTirKY8M?t=0)）
- [[ip-hash-load-balancing]] — IP hash 算法通过对客户端的IP地址取哈希值来决定路由目标服务器，从而保证该客户端后续所有请求都被稳定路由到同一台服务器。（[18:02](https://youtu.be/oYxTTirKY8M?t=1082)）
- [[jwt-bearer-token-claims]] — 用户完成身份认证后，系统通常签发 JWT 或 bearer token，其中携带 user ID、角色（如 admin、editor）、允许访问的 scope、过期时间以及签发者（issuer）等信息，用于支撑后续的授权判断。（[1:51:32](https://youtu.be/oYxTTirKY8M?t=6692)）
- [[jwt-token-authentication]] — JWT 本质上是一个经过签名的 JSON 对象，其中包含用户 ID 或邮箱等用于验证身份的信息，以及过期时间和角色、权限等其他自定义 claims。（[93:24](https://youtu.be/oYxTTirKY8M?t=5604)）
- [[key-value-store-nosql]] — 键值存储数据库（如 Redis、Memcached）最大的优势是简单和速度快，因为数据主要存储在 RAM 中，读写速度远快于其他类型数据库。（[10:22](https://youtu.be/oYxTTirKY8M?t=622)）
- [[least-connections-load-balancing]] — least connections 算法把新请求导向当前活跃连接数最少的服务器；例如三台服务器分别有10、9、30个活跃连接时，新请求会被导向活跃连接数为9的那台服务器。（[15:01](https://youtu.be/oYxTTirKY8M?t=901)）
- [[least-response-time-load-balancing]] — least response time 算法会优先把流量导向响应速度最快的服务器，但当该服务器的活跃连接数达到某个阈值（如40个）后，会切换到响应速度次之的服务器分担部分流量（如20个请求），实现响应速度与负载的动态平衡。（[18:02](https://youtu.be/oYxTTirKY8M?t=1082)）
- [[load-balancer-fault-tolerance]] — 当某台服务器（如 server 3）宕机时，负载均衡器会停止向其发送流量，把所有请求转移给剩余可用的服务器，直到该服务器重新恢复可用。（[15:01](https://youtu.be/oYxTTirKY8M?t=901)）
- [[load-balancer-health-monitoring]] — 可以像 load balancer 对服务器做健康检查一样，对 load balancer 自身持续做健康检查，一旦检测到某个 load balancer 宕机，就不再向其转发流量，直至其恢复在线。（[27:03](https://youtu.be/oYxTTirKY8M?t=1623)）
- [[load-balancer-need-multi-server]] — 当系统从单台服务器扩展为多台服务器（如三台）后，需要引入一个中间组件来决定每个客户端请求（无论来自mobile app还是desktop）应路由到哪台服务器。（[12:00](https://youtu.be/oYxTTirKY8M?t=720)）
- [[load-balancer-redundancy]] — 为避免 load balancer 成为单点故障，可部署多个 load balancer：当一个宕机时将全部流量重定向到另一个，待其恢复后再按比例（如50%）重新分配流量。（[27:03](https://youtu.be/oYxTTirKY8M?t=1623)）
- [[managed-load-balancer-provisioning]] — 相比自行搭建配置软件或硬件负载均衡器，AWS Elastic Load Balancing 这类云托管负载均衡器配置更为简便，若服务器本身也部署在 AWS 上则更加方便。（[24:03](https://youtu.be/oYxTTirKY8M?t=1443)）
- [[message-queue-exchange-types]] — 消息队列的 exchange 类型主要分为 direct（一对一）、fan-out（广播）和 topic-based（基于主题）三种。（[54:11](https://youtu.be/oYxTTirKY8M?t=3251)）
- [[mobile-api-json-response]] — 移动端 App 通过 HTTP 向服务器发起 API 请求获取数据，响应普遍采用 JSON 格式，因为 JSON 体积小且便于移动设备解析和使用。（[03:00](https://youtu.be/oYxTTirKY8M?t=180)）
- [[multi-model-authorization-combination]] — 真实系统常常结合多种 authorization model 一起使用，以便在灵活性和安全性之间取得平衡。（[1:54:33](https://youtu.be/oYxTTirKY8M?t=6873)）
- [[multiple-authorization-models-combination]] — 各authorization模型各有取舍，实际系统往往会把 RBAC、ABAC、ACL 等多种模型组合使用，以满足具体安全需求，而非只采用单一模型。（[1:45:30](https://youtu.be/oYxTTirKY8M?t=6330)）
- [[nginx-load-balancer]] — Nginx 是最常见的软件负载均衡器，通常直接安装在服务器上，通过配置指定需要被均衡的服务器列表以及使用的负载均衡算法。（[24:03](https://youtu.be/oYxTTirKY8M?t=1443)）
- [[nosql-database]] — NoSQL 数据库适用于需要 flexibility 以及对大量 unstructured data 进行快速访问的应用场景，与 RDBMS 是数据库选型的两大主流方向。（[06:00](https://youtu.be/oYxTTirKY8M?t=360)）
- [[nosql-database-types]] — NoSQL 数据库主要分为四类：document store（如 MongoDB）、wide column store（如 Cassandra）、key-value store（如 Redis）和 graph store（如 Neo4j）。（[09:23](https://youtu.be/oYxTTirKY8M?t=563)）
- [[nosql-flexible-scalable-storage-usecase]] — 如果应用需要为海量数据提供flexible和scalable的存储，例如推荐引擎以key-value格式存储用户活动数据，则应该选择NoSQL数据库。（[12:00](https://youtu.be/oYxTTirKY8M?t=720)）
- [[nosql-low-latency-usecase]] — 如果应用要求超低延迟（super low latency）的快速响应，则应该选择NoSQL数据库。（[12:00](https://youtu.be/oYxTTirKY8M?t=720)）
- [[nosql-schema-flexibility-advantage]] — 以 customer、order、product 场景为例，MongoDB 可以把用户数据、订单和产品信息全部存储在单个文档中，无需像关系型数据库那样通过 join 组合多张表。（[10:42](https://youtu.be/oYxTTirKY8M?t=642)）
- [[nosql-unstructured-semistructured-usecase]] — 如果数据是非结构化或半结构化的（如JSON对象），且数据之间的relationship并不那么关键，则应该选择NoSQL数据库。（[12:00](https://youtu.be/oYxTTirKY8M?t=720)）
- [[oauth2-access-token]] — 拿到 access token 后，应用就可以携带该 token 向 Google Drive API 请求文件并返回用户的文件列表，但 token 本身不包含「你是谁」的信息。（[99:28](https://youtu.be/oYxTTirKY8M?t=5968)）
- [[oauth2-authorization-code-flow]] — OAuth 2 流程中，用户被重定向到资源方（如 Google）的 consent screen 查看权限请求，一旦用户同意，资源方会向发起请求的应用返回一个 authorization code，随后该应用用这个 code 去交换出真正可用于读取数据的 access token。（[96:26](https://youtu.be/oYxTTirKY8M?t=5786)）
- [[oauth2-authorization-framework]] — OAuth 2 本质上是 authorization framework 而非 authentication 方法，用于授权第三方应用代表用户访问指定资源（如 Google Drive 中的文件），而不负责验证用户身份。（[96:26](https://youtu.be/oYxTTirKY8M?t=5786)）
- [[oauth2-authorization-framework-misconception]] — OAuth 2 经常被误当作一种 authentication 方法，但它实际上是一个 authorization 框架。（[84:20](https://youtu.be/oYxTTirKY8M?t=5060)）
- [[oauth2-delegated-authorization]] — OAuth 2 是一种委托授权（delegated authorization）协议，当一个服务需要代表用户访问另一服务的资源时使用。（[1:51:32](https://youtu.be/oYxTTirKY8M?t=6692)）
- [[offset-based-pagination]] — 部分 API 用 offset 取代 page 参数实现分页：offset 告诉 API 从数据集的第几项开始计数，再配合 limit 限制本次返回的条目数量。（[1:06:15](https://youtu.be/oYxTTirKY8M?t=3975)）
- [[oidc-id-token]] — 应用用 authorization code 换取 token 时会同时拿到 access token 和 ID token：access token 用于 OAuth 2 授权访问资源，ID token 则是包含用户身份的 JWT。（[99:28](https://youtu.be/oYxTTirKY8M?t=5968)）
- [[opaque-token-pre-jwt]] — 在 JWT 出现之前，token 只是一个不携带任何信息的字符串，服务端每次使用时都必须在数据库或缓存中查找该 token 才能验证用户权限，本质上仍是有状态的。（[93:24](https://youtu.be/oYxTTirKY8M?t=5604)）
- [[openid-connect]] — OpenID Connect 在 OAuth 2 的授权能力之上增加了认证（authentication）能力，这是二者的核心区别。（[99:28](https://youtu.be/oYxTTirKY8M?t=5968)）
- [[over-fetching-under-fetching]] — REST 采用资源导向（resource-based）的端点设计（如 users、followers、posts 各自独立端点），因此获取像用户详情、帖子、关注者这类关联数据时，通常需要分别发起多次请求才能拿到全部所需数据。（[33:05](https://youtu.be/oYxTTirKY8M?t=1985)）
- [[overall-rate-limiting-ddos-protection]] — 当进入服务器的总流量超过预设的全局阈值时，系统会临时封锁所有请求，直到定位到问题根源为止。（[1:57:34](https://youtu.be/oYxTTirKY8M?t=7054)）
- [[pagination-api-design]] — 当数据量很大（如成千上万篇帖子）时，API 应通过带 limit 和 offset 的分页返回结果，而不是一次性返回所有数据。（[40:20](https://youtu.be/oYxTTirKY8M?t=2420)）
- [[parameterized-queries-orm-defense]] — 防御 SQL/NoSQL injection 的标准做法是始终使用参数化查询（parameterized queries）或依赖 ORM 提供的安全防护，而不是直接把用户输入拼接进查询字符串。（[1:57:34](https://youtu.be/oYxTTirKY8M?t=7054)）
- [[polling-technique]] — 轮询方式在没有新数据时仍会产生完整的请求-响应往返，导致延迟增加、带宽浪费在空响应上，并且无谓消耗服务器资源。（[51:09](https://youtu.be/oYxTTirKY8M?t=3069)）
- [[postman-auth-type-terminology-confusion]] — Postman 在请求的 Authorization 标签页中把 Basic authentication、Digest authentication、API key 等都统一标注为 authentication type，但实际上其中一些属于 authentication method，另一些（如 OAuth）属于 authorization framework，这种命名方式容易让开发者混淆二者的区别。（[87:22](https://youtu.be/oYxTTirKY8M?t=5242)）
- [[protocol-influences-api-design]] — 通信协议的选择会影响 API 的设计方式，并贯穿整个 API 设计流程。（[45:08](https://youtu.be/oYxTTirKY8M?t=2708)）
- [[protocol-selection-importance]] — 为 API 选错协议可能导致性能瓶颈，也会限制功能实现，因此需要先理解各协议特性再做选择。（[45:08](https://youtu.be/oYxTTirKY8M?t=2708)）
- [[public-facing-api]] — public facing API 允许来自互联网的任意用户请求，而 VPN 内部 API 只允许同一网络内的调用方通过检查并到达。（[2:00:34](https://youtu.be/oYxTTirKY8M?t=7234)）
- [[rate-limiting]] — rate limiting 通过为客户端设置单位时间内的请求次数上限（如每段时间100次），超出该上限的请求会被拒绝，需等待一段时间后才能再次发起请求。（[1:54:33](https://youtu.be/oYxTTirKY8M?t=6873)）
- [[rate-limiting-per-endpoint]] — rate limiting 可以按端点粒度设置，例如对「/comments」这类端点单独设置更严格的每分钟请求次数限制。（[1:54:33](https://youtu.be/oYxTTirKY8M?t=6873)）
- [[rate-limiting-per-user-ip]] — rate limiting 也可以按用户或 IP 地址粒度设置，一旦某个 IP 的请求次数超过限额（如第101次请求），该 IP 就会被封锁。（[1:54:33](https://youtu.be/oYxTTirKY8M?t=6873)）
- [[real-time-communication-alternatives]] — HTTP 协议擅长处理请求-响应模式，但在需要频繁轮询数据的场景（如聊天应用）中存在局限性，这类需求通常需要借助 WebSocket 等协议来解决。（[51:00](https://youtu.be/oYxTTirKY8M?t=3060)）
- [[redis-in-ram-key-value-cache]] — Redis 是生产环境中存储 session 的常用首选方案，因为它读写速度快，并且原生支持 key 的过期（expiration）机制。（[90:23](https://youtu.be/oYxTTirKY8M?t=5423)）
- [[refresh-token-http-only-cookie-storage]] — refresh token 绝不应存储在 local storage，而应存储在 HTTP only cookie 中，以避免客户端被 XSS 攻击窃取长期有效的 refresh token。（[96:26](https://youtu.be/oYxTTirKY8M?t=5786)）
- [[relational-database-rdbms]] — 关系型数据库（RDBMS）的数据以表（table）的形式组织，是 SQL 数据库的基本构建单元，类似电子表格。（[06:00](https://youtu.be/oYxTTirKY8M?t=360)）
- [[relational-vs-nosql-selection-heuristic]] — 如果应用数据结构清晰、实体间存在明确关系（例如电商应用中追踪 customer 和 order），适合使用 SQL 数据库。（[11:05](https://youtu.be/oYxTTirKY8M?t=665)）
- [[rest-api-explicit-versioning]] — RESTful API 通常在 URL 中包含版本号如 /v1/，进行重大升级时会推出 /v2/ 等新版本，提供明确的版本控制（explicit versioning）。（[36:06](https://youtu.be/oYxTTirKY8M?t=2166)）
- [[rest-collection-vs-item-resource]] — 请求 /api/products 返回整个产品集合，而请求 /products/{id} 则返回该 ID 对应的单个产品项，二者共用同一资源名但语义不同。（[63:13](https://youtu.be/oYxTTirKY8M?t=3793)）
- [[rest-http-caching-headers]] — RESTful API 可以利用请求中的 headers 来实现标准的 HTTP caching。（[36:06](https://youtu.be/oYxTTirKY8M?t=2166)）
- [[rest-http-method-url-mapping]] — 删除资源应使用 DELETE 方法请求 /users/{id}，而不应使用 POST 请求 /users/{id} 或类似 /delete 这样的动作式 URL，HTTP 方法和 URL 结构都必须正确设置。（[75:30](https://youtu.be/oYxTTirKY8M?t=4530)）
- [[rest-nested-resource-url]] — 要获取某个特定产品的评论，可以设计嵌套路径 /products/{id}/reviews，请求该路径即可返回该产品对应的评论集合。（[63:13](https://youtu.be/oYxTTirKY8M?t=3793)）
- [[rest-noun-based-url-design]] — 在 RESTful API 设计中，业务领域概念（如 product、order、review）应转换为名词形式的资源路径，而不是像 get products 这样的动词式端点。（[63:13](https://youtu.be/oYxTTirKY8M?t=3793)）
- [[rest-plural-noun-convention]] — 在 RESTful API 设计中应统一使用复数名词命名资源路径，例如 /products 而不是 /product，用来表示该资源的集合。（[75:17](https://youtu.be/oYxTTirKY8M?t=4517)）
- [[rest-statelessness]] — REST API 的一大优势是无状态（stateless）：每个请求都包含处理该请求所需的全部信息，不需要依赖任何先前的请求即可被独立处理。（[30:03](https://youtu.be/oYxTTirKY8M?t=1803)）
- [[restful-api-design]] — 一个典型的 REST 风格接口示例是 GET /product/{id}，用于获取某个产品的详情，服务端返回的 JSON 中通常包含 ID、名称、描述、价格等元数据字段供客户端展示。（[03:00](https://youtu.be/oYxTTirKY8M?t=180)）
- [[role-based-access-control]] — 在 RBAC 中，editor 角色权限通常略低于 admin：可以创建、读取和更新内容，但不能删除资源，也不能管理其他用户。（[1:48:30](https://youtu.be/oYxTTirKY8M?t=6510)）
- [[round-robin-load-balancing]] — round robin 是最简单的负载均衡算法：按顺序依次把请求分配给服务器池中的各台服务器，轮到最后一台后再从第一台重新开始循环。（[15:01](https://youtu.be/oYxTTirKY8M?t=901)）
- [[saml]] — SAML 是一种基于 XML 的身份协议，常用于 Salesforce、企业仪表盘等 enterprise 和 legacy 系统中的认证场景。（[1:42:29](https://youtu.be/oYxTTirKY8M?t=6149)）
- [[self-healing-system]] — 自愈系统会持续监控 load balancer 等组件的健康状态，一旦发现其宕机，就自动启动一个同类型的新实例替换它，从而不中断客户端连接。（[27:03](https://youtu.be/oYxTTirKY8M?t=1623)）
- [[session-based-authentication]] — session-based authentication 的典型流程是：用户登录后服务器生成 session ID 并通过 cookie 下发给客户端，后续请求携带该 cookie 时服务器会在 session storage 中查找会话，找到则视为已认证并返回用户数据，找不到则返回 unauthorized。（[90:23](https://youtu.be/oYxTTirKY8M?t=5423)）
- [[session-storage-options]] — session storage 可以简单地用内存变量实现，但服务器重启或崩溃后数据会丢失；也可以用 Redis 或 SQL 数据库持久化存储；用服务器本地文件系统存储的方式非常少见，且不具备可扩展性。（[90:23](https://youtu.be/oYxTTirKY8M?t=5423)）
- [[single-point-of-failure]] — 仅依赖vertical scaling的单台服务器若宕机，将没有其他服务器可用，导致整个应用随之下线，这正是单点故障（single point of failure）风险的体现。（[12:00](https://youtu.be/oYxTTirKY8M?t=720)）
- [[single-server-scalability-limit]] — 单服务器架构适合小规模用户场景，但在高并发或大流量下会难以支撑，这正是后续需要引入横向扩展、负载均衡等手段的原因。（[03:00](https://youtu.be/oYxTTirKY8M?t=180)）
- [[single-server-setup]] — 在单服务器架构中，web 应用、数据库、缓存等组件全部运行在同一台服务器上，这是系统设计从零开始最基本的起点，之后再随用户规模增长逐步演化。（[00:00](https://youtu.be/oYxTTirKY8M?t=0)）
- [[single-sign-on]] — 例如登录 Google 或 Okta 一次后，用户无需再次登录即可访问 Gmail、Google Drive、YouTube、Google Calendar 等多个服务。（[99:28](https://youtu.be/oYxTTirKY8M?t=5968)）
- [[spof-scalability-risk]] — 系统中存在单点故障会拖累可扩展性，因为每添加一个新组件都会引入新的失效风险点。（[27:03](https://youtu.be/oYxTTirKY8M?t=1623)）
- [[spof-security-risk]] — 单点故障也是安全隐患：攻击者可以针对该单点（如 load balancer）发送大量流量，一旦其失效，整个系统随之瘫痪。（[27:03](https://youtu.be/oYxTTirKY8M?t=1623)）
- [[sql-injection-attack]] — SQL/NoSQL injection 的风险在于攻击者构造的输入能完全绕过应用原本的校验逻辑，直接对数据库执行任意读取、修改或删除操作，甚至删除全部用户数据和其他数据表。（[1:57:34](https://youtu.be/oYxTTirKY8M?t=7054)）
- [[sql-join-operation]] — SQL 支持跨多张表的复杂 join 操作，例如把 customers 表和 products 表连接（join）成一张 orders 表，用于记录哪个客户订购了哪些产品。（[06:00](https://youtu.be/oYxTTirKY8M?t=360)）
- [[sql-strong-consistency-usecase]] — 金融应用或银行系统这类需要strong consistency和事务完整性（transactional integrity）的场景，应该使用SQL数据库。（[12:00](https://youtu.be/oYxTTirKY8M?t=720)）
- [[sql-table-structure]] — SQL 表中的列（column）对应字段或属性（如 ID、name、age、email），行（row）对应表中的单条具体记录（如某个具体客户）。（[06:00](https://youtu.be/oYxTTirKY8M?t=360)）
- [[sso-ux-pattern-misconception]] — single sign-on 经常被误当作一种 authentication 方法，但它实际上是一种用户体验模式（UX pattern）。（[84:20](https://youtu.be/oYxTTirKY8M?t=5060)）
- [[start-small-system-design-principle]] — 系统设计学习应先从最简单直接的单服务器搭建开始，理解系统架构的基本组成部分，再逐步探索如何扩展系统各部分以支撑更多用户。（[03:00](https://youtu.be/oYxTTirKY8M?t=180)）
- [[status-code-selection-best-practice]] — 设计 REST API 时应根据每个操作的实际结果选用最贴切的状态码（如创建用 201、查询成功用 200、未找到用 404），而不是笼统地都返回同一个状态码。（[72:17](https://youtu.be/oYxTTirKY8M?t=4337)）
- [[stored-xss-comment-injection]] — 若 API 未对用户提交的评论内容做过滤就直接存入数据库，攻击者可在评论中植入脚本，之后其他用户加载该评论时，浏览器会在其页面环境中执行这段被注入的恶意 JavaScript，可能用于窃取其他用户的 cookie 或篡改数据库。（[2:00:34](https://youtu.be/oYxTTirKY8M?t=7234)）
- [[system-design-interview-importance]] — 科技公司面试中普遍加入 system design 环节，用于测试候选人能否在高层理解系统运作、做出规模化的架构决策，并清晰阐述自己参与项目中做出的取舍（trade-offs）。（[00:00](https://youtu.be/oYxTTirKY8M?t=0)）
- [[tcp-protocol]] — TCP 是比 UDP 更安全可靠但速度更慢的协议，需要先完成连接建立（握手）才能开始数据传输。（[60:11](https://youtu.be/oYxTTirKY8M?t=3611)）
- [[tcp-three-way-handshake]] — TCP 三步握手流程为：第一步客户端向服务器发送请求；第二步服务器 SYNC 并 ACK 该请求；第三步客户端向服务器发送 ACK，至此客户端与服务器之间的连接建立完成，双方才能在此连接上开始收发数据。（[60:11](https://youtu.be/oYxTTirKY8M?t=3611)）
- [[tcp-udp-selection-criteria]] — 选择协议时的核心判断标准是：若需要连接安全可靠则选 TCP；若需要快速轻量、可以接受一定数据丢失则选 UDP。（[60:11](https://youtu.be/oYxTTirKY8M?t=3611)）
- [[tcp-use-cases]] — 银行业务、电子邮件、支付等场景最适合使用 TCP，因为这些场景要求数据必须完整可靠地送达。（[60:11](https://youtu.be/oYxTTirKY8M?t=3611)）
- [[token-based-authentication]] — 与 session-based authentication 不同，token-based authentication 让客户端在每次请求中附带 token（而非依赖服务器记住会话），是现代应用的主流认证方式。（[93:24](https://youtu.be/oYxTTirKY8M?t=5604)）
- [[token-vs-authorization-model-distinction]] — token 通常携带用户的身份和声明信息，而 authorization model（如 RBAC、ABAC）才是定义用户可访问内容的规则，二者属于不同层面的概念，不应混淆。（[1:54:33](https://youtu.be/oYxTTirKY8M?t=6873)）
- [[top-down-api-design-approach]] — top-down 设计方式从高层需求和工作流出发，先给定需求再逐步定义 endpoint 与 operation，是面试中常见的 API 设计方式。（[42:08](https://youtu.be/oYxTTirKY8M?t=2528)）
- [[transport-layer-tcp-udp]] — REST、gRPC、WebSocket 等应用层协议都建立在传输层之上，而传输层包含 TCP 和 UDP，具体选用哪一种取决于所构建 API 的需求。（[57:11](https://youtu.be/oYxTTirKY8M?t=3431)）
- [[udp-protocol]] — UDP 不保证所有数据包都能送达，例如发送四个数据包，其中一个丢失后不会被重新推送给客户端，UDP 本身不会确保它最终被送达。（[60:11](https://youtu.be/oYxTTirKY8M?t=3611)）
- [[udp-use-cases]] — 视频通话、在线游戏和直播是 UDP 的典型应用场景：即使某个数据包丢失（如通话中对方网络卡顿导致片段丢失），也无需重新获取这部分旧数据，可以直接跳过继续处理后续数据包。（[60:11](https://youtu.be/oYxTTirKY8M?t=3611)）
- [[unauthorized-401-status-code]] — HTTP 401 属于 4xx 客户端错误系列，表示用户未通过身份验证、无权发起该请求。（[72:17](https://youtu.be/oYxTTirKY8M?t=4337)）
- [[unprotected-api-attack-risk]] — 若不对 API 设置 rate limiting，攻击者可以每分钟发送数千请求压垮系统，或借此对数据进行暴力破解。（[1:54:33](https://youtu.be/oYxTTirKY8M?t=6873)）
- [[vertical-scaling]] — vertical scaling（也称scale up）指为现有服务器增加RAM、CPU等资源来提升处理能力，实现简单，适合流量低到中等的应用。（[12:00](https://youtu.be/oYxTTirKY8M?t=720)）
- [[vpn-api-access-restriction]] — VPN 网络内的 API 只能被同处该网络的调用方访问，来自公网、不在该网络内的用户请求会被直接拦截。（[2:00:34](https://youtu.be/oYxTTirKY8M?t=7234)）
- [[vpn-internal-tool-use-case]] — 内部 admin 后台的 API 通常只对接入公司 VPN 的员工开放，是 VPN 隔离网络访问的典型应用场景。（[2:00:34](https://youtu.be/oYxTTirKY8M?t=7234)）
- [[vpn-private-api-access]] — 对于只应被特定网络访问的私有 API，可以使用 VPN 来限制其可访问范围，而非直接对公网开放。（[1:57:34](https://youtu.be/oYxTTirKY8M?t=7054)）
- [[web-application-firewall]] — 以 AWS WAF 为例，firewall 可以基于可疑 SQL 关键字、异常 HTTP 方法等已知攻击模式识别并拦截恶意请求，同时允许正常流量顺利到达 API。（[1:57:34](https://youtu.be/oYxTTirKY8M?t=7054)）
- [[web-mobile-traffic-sources]] — 对 web 用户，服务器需要同时处理业务逻辑、数据存储，以及用 HTML、CSS、JavaScript 完成的页面呈现；而对 mobile 用户，服务器主要通过 HTTP API 调用返回数据，界面渲染由客户端自行完成。（[03:00](https://youtu.be/oYxTTirKY8M?t=180)）
- [[web-tier-data-tier-separation]] — 将单体服务器拆分为 web tier 和 data tier 后，可以根据各层各自的负载分别进行扩展（scale），而不必整体一起扩容。（[06:00](https://youtu.be/oYxTTirKY8M?t=360)）
- [[websocket-communication]] — WebSocket 支持双向实时通信，适合聊天应用、视频流等需要实时数据交互的场景。（[41:40](https://youtu.be/oYxTTirKY8M?t=2500)）
- [[websocket-handshake]] — WebSocket 的 handshake 只发生在第一次请求，完成之后客户端与服务端之间就建立了持久的双向通信通道。（[51:09](https://youtu.be/oYxTTirKY8M?t=3069)）
- [[websocket-server-push]] — handshake 完成后，一旦服务端产生了新数据，它可以主动推送给客户端而不必等待客户端发起请求，从而实现最低延迟的实时数据更新。（[51:09](https://youtu.be/oYxTTirKY8M?t=3069)）
- [[weighted-load-balancing-variant]] — 加权类负载均衡算法（如 weighted round robin、weighted least connections）会根据服务器的硬件容量指标（例如16GB、32GB、64GB的RAM）为每台服务器分配不同权重，权重更高的服务器会被分配更多比例的流量。（[18:02](https://youtu.be/oYxTTirKY8M?t=1082)）
- [[weighted-round-robin-load-balancing]] — 在 weighted round robin 算法中，权重更高（性能更强）的服务器会被分配更多连接，权重较低的服务器只接收较小比例的流量。（[21:03](https://youtu.be/oYxTTirKY8M?t=1263)）
- [[wide-column-store-database]] — 宽列存储数据库（如 Cassandra、Cosmos DB）将数据组织为表、行和动态列，擅长处理海量数据规模并对大量写操作有很好的支持。（[09:48](https://youtu.be/oYxTTirKY8M?t=588)）
- [[worker-pull-pattern]] — 以订单处理为例：producer 在新订单产生时向队列发布消息，consumer 仅在自己有空闲处理能力时才从队列中拉取消息并更新库存数据；若 consumer 正忙，消息会留在队列中等待其下次有空闲容量时再被拉取处理。（[54:11](https://youtu.be/oYxTTirKY8M?t=3251)）
- [[xss-attack]] — XSS 让攻击者能够把脚本注入进最终会呈现给其他用户的网页内容中，从而在受害者浏览器环境中执行恶意代码。（[2:00:34](https://youtu.be/oYxTTirKY8M?t=7234)）
